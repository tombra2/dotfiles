# Application bindings
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" tmux new
$terminal = uwsm-app -- xdg-terminal-exec
$browser = omarchy-launch-browser


unbind = SUPER ,W
unbind = SUPER, TAB
unbind = SUPER, J 
unbind = SUPER, L 
unbind = SUPER, P
unbind = SUPER, C
unbind = SUPER, K
unbind = , PRINT
unbind = SHIFT, PRINT
unbind = ALT, PRINT
unbind = SUPER, PRINT
unbind = SUPER, LEFT
unbind = SUPER, RIGHT

bindd = SUPER, RETURN, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER , E, File manager, exec, omarchy-launch-tui yazi
bindd = SUPER , B, Browser, exec, $browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, $browser --privlate
bindd = SUPER SHIFT, T, Activity, exec, omarchy-launch-tui btop
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus "^obsidian$" "uwsm-app -- obsidian"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password


bind = SUPER, Tab, workspace, previous
bind = SUPER, Q, killactive
bindd = SUPER, P, Screenshot with editing, exec, omarchy-cmd-screenshot
bindd = SUPER, C, Color picker, exec, pkill hyprpicker || hyprpicker -a

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER , A, Claude, exec, omarchy-launch-webapp "https://claude.ai"
#
# # Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu
 bind = SUPER, h, movefocus, l
 bind = SUPER, l, movefocus, r
 bind = SUPER, k, movefocus, u
 bind = SUPER, j, movefocus, d

# Resize active window
bind = SUPER, LEFT,  resizeactive, -100 0    # - key
bind = SUPER, RIGHT, resizeactive, 100 0     # = key

#
